{
  "name": "sitepoemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sitemap": "node --env-file=.env.local scripts/generate-sitemap.js || node scripts/generate-sitemap.js",
    "build": "npm run sitemap && vite build && cp 404.html manifest.json favicon.svg .nojekyll dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.7.1",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "html2canvas": "^1.4.1"
  }
}
